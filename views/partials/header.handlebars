<header>
    <div class="container-fluid">

        <div class="row">

            <div class="col-md-7 p-3">
                <h1>
                    <a href="/"> JAVA RATINGS </a>
                </h1>
            </div>

            <div class="col-md-5 p-3">
                <nav>
                    
                        {{!-- look in routes/view-routes.js for the data --}}
                        {{!-- anchors partial is in views/partials/anchor.handlebars --}}
                            {{#if user}}
                            {{#with loggedIn}}
                            {{> anchor}}
                            {{/with}}
                            {{else}}
                            {{#with loggedOut}}
                            {{> anchor}}
                            {{/with}}
                            {{/if}}
                      
                </nav>
            </div>

        </div>

        <div class="row">
            <div class="col-md-12 p-3">
                <div class="tagline">
                    <em>cultivating a community of coffee connoisseurs</em>
                </div>
            </div>
        </div>
    </div>
</header>